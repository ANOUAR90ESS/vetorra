# ✅ Checklist - التحقق من الجاهزية للنشر

## 📋 قبل النشر

### 1. المتغيرات البيئية
- [ ] فتح Vercel Dashboard → Settings → Environment Variables
- [ ] إضافة `GEMINI_API_KEY` مع القيمة الفعلية
- [ ] التأكد من عدم وجود بادئة `VITE_` في اسم المتغير
- [ ] (اختياري) حذف `VITE_GEMINI_API_KEY` إن وجدت

### 2. الملفات المحلية
- [ ] **لا تنشئ** ملف `.env` محلي إلا للتطوير
- [ ] إذا أنشأت `.env`، تأكد أنه **ليس** في Git
- [ ] تحقق: `git status` يجب ألا يظهر `.env`

### 3. Git Commit
```bash
git add .
git commit -m "🔒 Move Gemini API to serverless backend"
git push
```

### 4. الانتظار للـ Deploy
- [ ] انتظر حتى ينتهي Vercel من البناء (Build)
- [ ] افتح Vercel Dashboard → Deployments
- [ ] تحقق من أن Status = "Ready"

### 5. الاختبار
- [ ] افتح موقعك المنشور
- [ ] جرب توليد خبر جديد (News)
- [ ] جرب توليد أداة جديدة (Tool)
- [ ] جرب إنشاء صورة AI
- [ ] افتح Browser Console وتأكد من عدم وجود أخطاء

---

## 🔍 استكشاف الأخطاء

### ❌ خطأ: "API request failed"
**السبب المحتمل**: مفتاح API غير موجود أو خاطئ

**الحل**:
1. اذهب إلى Vercel → Settings → Environment Variables
2. تأكد من وجود `GEMINI_API_KEY`
3. تحقق من صحة القيمة (انسخها من Google AI Studio)
4. **مهم**: اضغط "Redeploy" بعد التعديل

### ❌ خطأ: "Cannot find module '@google/genai'"
**السبب المحتمل**: لم يتم تحديث `geminiService.ts`

**الحل**: تأكد من تنفيذ جميع التعديلات المطلوبة

### ❌ خطأ: "404 on /api/gemini"
**السبب المحتمل**: ملف `/api/gemini.ts` لم ينشر

**الحل**:
```bash
# تأكد من وجود الملف
ls api/gemini.ts

# إذا موجود، أعد النشر
git add api/
git commit -m "Add serverless Gemini API"
git push
```

### ❌ الصور لا تعمل (fallback فقط)
**السبب المحتمل**: خطأ في توليد الصور من Gemini

**الحل**:
1. افتح Vercel → Functions Logs
2. ابحث عن أخطاء تحتوي على "generateImage"
3. تحقق من أن Model `gemini-1.5-flash-latest` يدعم الصور

---

## 📊 مراقبة الأداء

### Vercel Dashboard → Functions
- تحقق من عدد الطلبات (Invocations)
- تابع الأخطاء (Errors)
- راقب وقت التنفيذ (Duration)

### متوسط الأوقات المتوقعة:
- توليد نص (News/Tools): 2-5 ثوانٍ
- توليد صورة: 3-8 ثوانٍ
- البحث الذكي: 1-3 ثوانٍ

---

## 🚀 بعد النشر الناجح

### اختياري - تحسينات مستقبلية:

1. **Rate Limiting**
   - إضافة حد للطلبات لكل مستخدم/IP
   - منع إساءة استخدام الـ API

2. **Caching**
   - حفظ النتائج المكررة
   - تقليل تكلفة Gemini API

3. **Error Monitoring**
   - ربط Sentry أو Datadog
   - تنبيهات تلقائية للأخطاء

4. **CORS Restrictions**
   - تحديد النطاقات المسموحة
   - حالياً: السماح لأي نطاق (`*`)

---

## ✅ علامات النجاح

إذا رأيت كل هذه العلامات، فالنشر **ناجح 100%**:

✅ موقعك يفتح بدون أخطاء
✅ توليد الأخبار يعمل
✅ توليد الأدوات يعمل
✅ الصور تُنشأ (ليست fallback فقط)
✅ لا أخطاء في Browser Console
✅ لا أخطاء في Vercel Function Logs
✅ مفتاح `GEMINI_API_KEY` غير ظاهر في Network Tab

---

**مبروك! 🎉 موقعك الآن آمن وجاهز للإنتاج**
