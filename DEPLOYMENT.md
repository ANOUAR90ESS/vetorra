# 🚀 دليل النشر - نقل Gemini API إلى الخادم

## ✅ ما تم إنجازه

تم نقل جميع استدعاءات Gemini API من العميل (Frontend) إلى الخادم (Backend) لحماية مفتاح API.

### الملفات المعدلة:
1. **`/api/gemini.ts`** - API موحد لجميع عمليات Gemini
2. **`/services/geminiService.ts`** - تم تحويل جميع الوظائف لاستخدام `/api/gemini`
3. **`.env.example`** - تم تحديث المتغيرات البيئية

---

## 📋 خطوات النشر على Vercel

### 1️⃣ إعداد المتغيرات البيئية

في لوحة تحكم Vercel:
1. اذهب إلى **Settings** → **Environment Variables**
2. أضف المتغير التالي:

```
GEMINI_API_KEY=your_actual_gemini_api_key_here
```

⚠️ **مهم جداً**:
- لا تضف بادئة `VITE_` لهذا المتغير (لأنه خاص بالخادم فقط)
- احذف أو تجاهل `VITE_GEMINI_API_KEY` من البيئة (لم نعد نحتاجها)

### 2️⃣ Deploy إلى Vercel

بعد إضافة المتغير البيئي، قم بالنشر:

```bash
# من خلال Git
git add .
git commit -m "Move Gemini API to serverless functions"
git push

# أو مباشرة
vercel --prod
```

### 3️⃣ التحقق من النجاح

بعد النشر، تحقق من:
1. افتح موقعك وجرب أي ميزة تستخدم Gemini
2. افتح Console في المتصفح - يجب ألا ترى أي أخطاء API
3. تحقق من Vercel Logs للتأكد من عمل `/api/gemini`

---

## 🔧 للتطوير المحلي

### إنشاء ملف `.env`

أنشئ ملف `.env` في جذر المشروع:

```bash
cp .env.example .env
```

ثم عدّل الملف وأضف مفاتيحك الفعلية:

```env
GEMINI_API_KEY=your_actual_gemini_api_key
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### تشغيل المشروع محلياً

```bash
npm run dev
```

⚠️ **ملاحظة**: إذا كنت تختبر محلياً، قد تحتاج لتشغيل Vercel Dev بدلاً من Vite:

```bash
npm install -g vercel
vercel dev
```

هذا يضمن عمل `/api/gemini` محلياً أيضاً.

---

## 📊 الوظائف المدعومة عبر API

جميع الوظائف التالية تعمل الآن عبر `/api/gemini`:

### ✅ News (الأخبار)
- `generateNewsDetails` - إنشاء خبر واحد
- `generateDirectoryNews` - إنشاء عدة أخبار دفعة واحدة
- `extractNewsFromRSSItem` - تحويل RSS إلى خبر

### ✅ Tools (الأدوات)
- `generateDirectoryTools` - إنشاء قائمة أدوات
- `generateToolDetails` - تفاصيل أداة واحدة
- `extractToolFromRSSItem` - تحويل RSS إلى أداة

### ✅ Images (الصور)
- `generateAIImage` - إنشاء صور AI
- `generateImage` - إنشاء صور بمواصفات محددة

### ✅ Chat (المحادثة)
- `sendChatMessage` - محادثة مع Gemini
- `intelligentSearch` - البحث الذكي

### ✅ Audio (الصوت)
- `transcribeAudio` - تحويل الصوت إلى نص
- `generateSpeech` - تحويل النص إلى صوت
- `generateConversationScript` - إنشاء سكريبت محادثة

### ⚠️ قيد التطوير
- `generateVideo` - توليد فيديو (يحتاج معالجة خاصة)
- `editImage` - تعديل الصور

---

## 🔐 الأمان

### ما تم تأمينه:
✅ مفتاح Gemini API لم يعد مكشوفاً في الـ Frontend
✅ جميع الطلبات تمر عبر `/api/gemini` الآمن
✅ CORS محدد للسماح بالطلبات من نطاقك فقط

### نصائح إضافية:
- لا تضع أبداً `GEMINI_API_KEY` في ملف يبدأ بـ `VITE_`
- تأكد من إضافة `.env` إلى `.gitignore`
- استخدم Environment Variables في Vercel فقط

---

## 🐛 استكشاف الأخطاء

### الخطأ: "Cannot find module '@google/genai'"
✅ **محلول** - لم نعد نستورد المكتبة في Frontend

### الخطأ: "API request failed"
🔍 **الحل**:
1. تحقق من إعداد `GEMINI_API_KEY` في Vercel
2. تحقق من Vercel Function Logs
3. تأكد من إعادة النشر بعد إضافة المتغير البيئي

### الخطأ: "fetch failed to /api/gemini"
🔍 **الحل**:
- في التطوير المحلي: استخدم `vercel dev` بدلاً من `npm run dev`
- في الإنتاج: تأكد من deploy الملف `/api/gemini.ts`

---

## 📝 ملاحظات مهمة

1. **CORS**: الإعدادات الحالية تسمح بكل الطلبات (`*`) - عدّلها لنطاقك فقط في الإنتاج
2. **Rate Limiting**: قد تحتاج لإضافة حد للطلبات لتجنب إساءة الاستخدام
3. **Error Handling**: تم إضافة معالجة أخطاء أساسية، قد تحتاج لتحسينها

---

## 🎯 الخطوات التالية (اختيارية)

1. **إضافة Rate Limiting**: لحماية API من الإساءة
2. **Caching**: لتقليل استدعاءات Gemini API المكررة
3. **Monitoring**: لمراقبة استخدام API
4. **إنهاء Video API**: لدعم توليد الفيديو بالكامل

---

تم بنجاح! 🎉
